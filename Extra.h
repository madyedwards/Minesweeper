#include <iostream>
#include <fstream>
#include <vector>
#include <vector>
using namespace std;

//this will be used to load in config file

struct Extra {
public:
    static vector<char> inputFile(int test_board);
    static vector<int> numbers(int value); //ta
};
